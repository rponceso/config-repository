#Application Configuration
server:
  port: 8084
spring:
  application:
    name: api-rest-credits
  data:
    mongodb:
      uri: mongodb+srv://user:pass@cluster-demo.wjgme.mongodb.net/credits-db
      port: 27017
      username: user
      password: pass      
  cloud:
    config:
      enabled: false
